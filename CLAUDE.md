# Repo Rules

- 親 `C:\Users\ramda\projects\CLAUDE.md` を先に適用する
- ビルド不要の HTML/JS/CSS 構成（PWA）。フレームワークやビルドツールを導入しない
- GitHub Pages で公開中（public repo）
- コミットメッセージは日本語で簡潔に書く
- 問題画像は `img/` に格納。ファイル名は出典の試験回と問番号に対応させる
